extends RefCounted
class_name GameConstants

# 游戏显示在窗口标题、菜单等位置的名称。
const GAME_NAME: String = "WanZong"

# 默认窗口宽度。
const DEFAULT_WINDOW_WIDTH: int = 1920

# 默认窗口高度。
const DEFAULT_WINDOW_HEIGHT: int = 1080

# 默认窗口尺寸，方便后续代码统一读取。
const DEFAULT_WINDOW_SIZE: Vector2i = Vector2i(DEFAULT_WINDOW_WIDTH, DEFAULT_WINDOW_HEIGHT)
